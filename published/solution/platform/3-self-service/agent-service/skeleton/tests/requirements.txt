# ABOUTME: What the generated service's contract tests need. Deliberately two lines.
# ABOUTME: These tests parse YAML and assert on it; they never contact the cluster.
#
# Installed by the command in the chapter:
#     pip install -r tests/requirements.txt
#     pytest -q tests/test_contract.py
#
# Pinned, matching the rest of this repository, and resolved on 2026-09-23. Raise them when you
# raise the rest; nothing here depends on a version-specific behavior.
pytest==9.1.1
PyYAML==6.0.3
