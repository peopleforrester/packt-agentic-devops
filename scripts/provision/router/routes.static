# Hosts the fleet driver does not manage (instructor standalone clusters).
# Rebuilt tables would otherwise drop these. Format: <hostname> <upstream-host:port>
#
# admin1/admin2 removed 2026-07-27: the instructor clusters were torn down, so their NLBs
# no longer exist and the routes would 502. Re-add a line here when an instructor cluster
# is next provisioned (its NLB hostname is the final line of instructor/up.sh output).
